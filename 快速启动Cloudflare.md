# 🚀 快速启动 Cloudflare Tunnel

## 步骤：

1. **确保开发服务器正在运行**
   ```bash
   npm run dev
   ```

2. **打开新的终端窗口，运行：**
   ```bash
   cd "/Users/lvcheng/Desktop/我的人生剧本-(my-life-script)"
   ./start-cloudflare-simple.sh
   ```

   或者直接运行：
   ```bash
   npx --yes cloudflared tunnel --url http://localhost:3000
   ```

3. **等待显示公网链接**
   会显示类似这样的内容：
   ```
   +----------------------------------------------------------------------------+
   |  Your quick Tunnel has been created! Visit it at (it may take some time):
   |  https://xxxx-xxxx-xxxx.trycloudflare.com
   +----------------------------------------------------------------------------+
   ```

4. **复制链接，在手机或其他电脑上打开**

---

## ⚠️ 重要提示

- **必须在新的终端窗口运行**，这样你才能看到输出的链接
- 保持这个终端窗口打开，关闭后链接会失效
- 这个链接可以在任何网络访问（包括手机移动网络）

---

## 💡 如果链接失效

重新运行 `./start-cloudflare-simple.sh` 即可获取新的链接。

