# 🇨🇳 部署到国内平台 - 中国大陆可访问

## 问题说明

Vercel 在中国大陆访问可能不稳定，导致手机无法打开。以下是几个国内可访问的解决方案：

---

## 🚀 方案 1：部署到 GitHub Pages（免费，推荐）

GitHub Pages 在中国大陆访问相对稳定，而且是免费的。

### 步骤：

#### 1. 创建 GitHub 仓库

1. 访问 https://github.com
2. 点击右上角 "+" → "New repository"
3. 输入仓库名称，例如：`my-life-script`
4. 选择 Public（GitHub Pages 免费版需要公开仓库）
5. 点击 "Create repository"

#### 2. 推送代码到 GitHub

在终端运行：

```bash
cd "/Users/lvcheng/Desktop/我的人生剧本-(my-life-script)"

# 初始化 Git（如果还没有）
git init

# 添加文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库（替换 YOUR_USERNAME 和 REPO_NAME）
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

#### 3. 配置 GitHub Pages

1. 在 GitHub 仓库页面，点击 **Settings**
2. 在左侧菜单找到 **Pages**
3. 在 **Source** 中选择 **GitHub Actions**
4. 或者选择 **main** 分支，文件夹选择 `/dist`

#### 4. 创建 GitHub Actions 工作流

在项目根目录创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm install
        
      - name: Build
        run: npm run build
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

#### 5. 配置 GitHub Secrets（API Key）

1. 在仓库页面，点击 **Settings** → **Secrets and variables** → **Actions**
2. 点击 **New repository secret**
3. 名称：`GEMINI_API_KEY`
4. 值：你的 API Key
5. 点击 **Add secret**

#### 6. 获取访问链接

部署完成后，访问链接格式为：
```
https://YOUR_USERNAME.github.io/REPO_NAME/
```

---

## 🚀 方案 2：部署到 Netlify（可能比 Vercel 好一些）

Netlify 在中国大陆的访问可能比 Vercel 稍好。

### 步骤：

```bash
# 安装 Netlify CLI
npm install -g netlify-cli

# 登录
netlify login

# 部署
netlify deploy --prod --dir=dist
```

### 配置环境变量：

1. 访问 https://app.netlify.com
2. 进入项目 → **Site settings** → **Environment variables**
3. 添加 `GEMINI_API_KEY`

---

## 🚀 方案 3：部署到阿里云 OSS（最稳定，需要账号）

### 步骤：

1. **登录阿里云控制台**
   - 访问 https://www.aliyun.com
   - 如果没有账号，先注册

2. **开通 OSS 服务**
   - 搜索 "对象存储 OSS"
   - 开通服务（有免费额度）

3. **创建 Bucket**
   - 点击 "创建 Bucket"
   - 设置名称
   - 选择区域（建议选择离你近的）
   - 读写权限选择 "公共读"

4. **开启静态网站托管**
   - 在 Bucket 设置中，找到 "静态网站托管"
   - 开启并设置默认首页为 `index.html`

5. **上传文件**
   - 进入 Bucket
   - 上传 `dist` 目录中的所有文件
   - 确保 `index.html` 在根目录

6. **获取访问地址**
   - 在 Bucket 概览中，找到 "访问域名"
   - 格式：`https://your-bucket-name.oss-region.aliyuncs.com`

7. **配置 CDN（可选，加速访问）**
   - 在 OSS 控制台，找到 "CDN 加速"
   - 开通 CDN 服务
   - 绑定自定义域名（如果有）

---

## 🚀 方案 4：部署到腾讯云 COS（类似阿里云）

### 步骤：

1. **登录腾讯云控制台**
   - 访问 https://cloud.tencent.com

2. **开通 COS 服务**
   - 搜索 "对象存储 COS"
   - 开通服务

3. **创建存储桶**
   - 设置名称和区域
   - 访问权限选择 "公有读私有写"

4. **开启静态网站**
   - 在存储桶设置中，开启 "静态网站"
   - 设置默认首页为 `index.html`

5. **上传文件**
   - 上传 `dist` 目录中的所有文件

6. **获取访问地址**
   - 在存储桶概览中找到访问域名

---

## 🚀 方案 5：使用 Coding Pages（国内 GitHub Pages 替代）

Coding 是腾讯的代码托管平台，类似 GitHub，在中国访问更快。

### 步骤：

1. 访问 https://coding.net
2. 注册账号
3. 创建项目
4. 推送代码
5. 开启 Pages 服务
6. 配置构建和部署

---

## 💡 推荐方案对比

| 方案 | 费用 | 访问速度 | 配置难度 | 推荐度 |
|------|------|----------|----------|--------|
| GitHub Pages | 免费 | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| Netlify | 免费 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| 阿里云 OSS | 有免费额度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 腾讯云 COS | 有免费额度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Coding Pages | 免费 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 🎯 快速开始（推荐 GitHub Pages）

如果你想要快速部署且免费，推荐使用 GitHub Pages：

1. 创建 GitHub 仓库
2. 推送代码
3. 配置 Pages
4. 完成！

需要我帮你创建 GitHub Actions 配置文件吗？

